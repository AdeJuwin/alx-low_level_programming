#include <stdio.h>

/**
* main - entry point
*Return: return 0 as success
*/

int main(void)
{
	int num = '0';
	int num = 0;

	while (num < '10')
		while (num < 10)
		{
			putchar(num);
			putchar(num + '0');
			num++;
		}
	putchar('\n');

	return (0);
}
